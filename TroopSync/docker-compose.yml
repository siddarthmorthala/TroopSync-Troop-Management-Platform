version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: troopsync
      POSTGRES_USER: troopsync
      POSTGRES_PASSWORD: troopsync
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgresql+psycopg://troopsync:troopsync@db:5432/troopsync
    ports:
      - "8000:8000"
    depends_on:
      - db

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  pgdata:
